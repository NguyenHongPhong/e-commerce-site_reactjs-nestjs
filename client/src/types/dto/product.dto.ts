export interface ICategoryDto {
    name?: string,
    description?: string,
    selectCategory?: string
};
export interface IProductDto {
    title?: string,
    colors?: string[],
    description?: string,
    materials?: string[],
    sizes?: string[],
    price?: string,

}
